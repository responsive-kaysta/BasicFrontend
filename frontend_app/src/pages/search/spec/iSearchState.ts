export interface iSearchState {
  loadingState: boolean;
  language: string;
  offline: string;
  deleted: string;
  searchTerm: string;
  loadingUrl: string;
  storeContainer: [];
}

export default iSearchState;
