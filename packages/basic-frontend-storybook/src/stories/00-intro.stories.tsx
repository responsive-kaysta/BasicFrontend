import { storiesOf } from "@storybook/react";
import * as React from "react";
import { Topbar } from "responsive-it-common";

storiesOf("00: Intro", module).add("Abstract", () => (
  <>
    <Topbar>Inhalte</Topbar>
  </>
));
